import { PaymentCancel } from '@/components/shared/Payment/cancel';

interface Props {}

const PaymentCancelPage: React.FC<Props> = (props) => {
  return <PaymentCancel />;
};

export default PaymentCancelPage;
