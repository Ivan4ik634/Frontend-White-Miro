import { BoardSettings } from '@/components/shared/BoardSettings';

interface Props {}

const BoardSettingsPage: React.FC<Props> = (props) => {
  return <BoardSettings />;
};
export default BoardSettingsPage;
